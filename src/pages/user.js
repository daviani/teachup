import Layout from '../component/layout/layout';

const User = () => {
    return (
        <Layout>
            <div>
                Welcome on User page
            </div>
        </Layout>
    );
};

export default User;
